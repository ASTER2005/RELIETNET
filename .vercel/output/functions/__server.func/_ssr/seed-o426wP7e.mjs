import { n as createMiddleware } from "./ssr.mjs";
//#region node_modules/.nitro/vite/services/ssr/assets/seed-o426wP7e.js
var _0001_auth_default = "-- Better Auth schema (identity + sessions for \"Sign in with Grok\").\n--\n-- Generated by the Better Auth CLI for its Postgres adapter — DO NOT EDIT by\n-- hand. `@/lib/auth/server` runs Better Auth against these tables when\n-- DATABASE_URL is set. The columns are camelCase and MUST stay double-quoted so\n-- Postgres preserves the case Better Auth queries by.\n--\n-- Migrations in this folder are the single source of truth for your schema. They\n-- apply to Neon during the Vercel build (`npm run build`) and to the local\n-- PGLite fallback automatically on startup, so dev matches production. Applied\n-- files are recorded by name in `_migrations` and NEVER run again.\n--\n-- Put YOUR app's schema in NEW ordered files (0002_*.sql, 0003_*.sql, …), never\n-- in this one. For app tables, prefer snake_case and give per-user tables a\n-- `user_id TEXT NOT NULL` column (TEXT, not UUID — the preview dev user id is\n-- the string 'dev-user'), then scope every query to the authenticated user\n-- server-side (see the `neon` + `auth` skills and src/lib/auth/verify.server.ts).\n\ncreate table if not exists \"user\" (\n  \"id\" text not null primary key,\n  \"name\" text not null,\n  \"email\" text not null unique,\n  \"emailVerified\" boolean not null,\n  \"image\" text,\n  \"createdAt\" timestamptz default CURRENT_TIMESTAMP not null,\n  \"updatedAt\" timestamptz default CURRENT_TIMESTAMP not null\n);\n\ncreate table if not exists \"session\" (\n  \"id\" text not null primary key,\n  \"expiresAt\" timestamptz not null,\n  \"token\" text not null unique,\n  \"createdAt\" timestamptz default CURRENT_TIMESTAMP not null,\n  \"updatedAt\" timestamptz not null,\n  \"ipAddress\" text,\n  \"userAgent\" text,\n  \"userId\" text not null references \"user\" (\"id\") on delete cascade\n);\n\ncreate table if not exists \"account\" (\n  \"id\" text not null primary key,\n  \"accountId\" text not null,\n  \"providerId\" text not null,\n  \"userId\" text not null references \"user\" (\"id\") on delete cascade,\n  \"accessToken\" text,\n  \"refreshToken\" text,\n  \"idToken\" text,\n  \"accessTokenExpiresAt\" timestamptz,\n  \"refreshTokenExpiresAt\" timestamptz,\n  \"scope\" text,\n  \"password\" text,\n  \"createdAt\" timestamptz default CURRENT_TIMESTAMP not null,\n  \"updatedAt\" timestamptz not null\n);\n\ncreate table if not exists \"verification\" (\n  \"id\" text not null primary key,\n  \"identifier\" text not null,\n  \"value\" text not null,\n  \"expiresAt\" timestamptz not null,\n  \"createdAt\" timestamptz default CURRENT_TIMESTAMP not null,\n  \"updatedAt\" timestamptz default CURRENT_TIMESTAMP not null\n);\n\ncreate index if not exists \"session_userId_idx\" on \"session\" (\"userId\");\ncreate index if not exists \"account_userId_idx\" on \"account\" (\"userId\");\ncreate index if not exists \"verification_identifier_idx\" on \"verification\" (\"identifier\");\n";
var _0002_relief_default = "-- Migration: RelietNet Schema\nCREATE TABLE IF NOT EXISTS users (\n  id TEXT PRIMARY KEY,\n  name TEXT NOT NULL,\n  role TEXT NOT NULL,\n  org_name TEXT,\n  location TEXT,\n  region TEXT,\n  contribution_type TEXT\n);\n\nCREATE TABLE IF NOT EXISTS requirements (\n  id TEXT PRIMARY KEY,\n  title TEXT NOT NULL,\n  camp_name TEXT NOT NULL,\n  receiver_id TEXT NOT NULL,\n  resource_type TEXT NOT NULL,\n  quantity_needed DOUBLE PRECISION NOT NULL,\n  quantity_fulfilled DOUBLE PRECISION NOT NULL,\n  quantity_unit TEXT NOT NULL,\n  people_affected INTEGER NOT NULL,\n  duration_days INTEGER NOT NULL,\n  urgency INTEGER NOT NULL,\n  location TEXT NOT NULL,\n  map_x DOUBLE PRECISION NOT NULL,\n  map_y DOUBLE PRECISION NOT NULL,\n  notes TEXT,\n  created_at TEXT NOT NULL,\n  status TEXT NOT NULL,\n  priority TEXT NOT NULL,\n  priority_score DOUBLE PRECISION NOT NULL,\n  score_breakdown JSONB NOT NULL\n);\n\nCREATE TABLE IF NOT EXISTS surplus (\n  id TEXT PRIMARY KEY,\n  camp_name TEXT NOT NULL,\n  receiver_id TEXT NOT NULL,\n  resource_type TEXT NOT NULL,\n  quantity DOUBLE PRECISION NOT NULL,\n  quantity_unit TEXT NOT NULL,\n  location TEXT NOT NULL,\n  map_x DOUBLE PRECISION NOT NULL,\n  map_y DOUBLE PRECISION NOT NULL,\n  notes TEXT,\n  created_at TEXT NOT NULL,\n  matched_to_requirement_id TEXT,\n  status TEXT NOT NULL\n);\n\nCREATE TABLE IF NOT EXISTS transactions (\n  id TEXT PRIMARY KEY,\n  kind TEXT NOT NULL,\n  requirement_id TEXT NOT NULL,\n  donor_id TEXT NOT NULL,\n  donor_name TEXT NOT NULL,\n  coordinator_id TEXT,\n  coordinator_name TEXT,\n  receiver_id TEXT NOT NULL,\n  receiver_name TEXT NOT NULL,\n  camp_name TEXT NOT NULL,\n  resource_type TEXT NOT NULL,\n  quantity DOUBLE PRECISION NOT NULL,\n  quantity_unit TEXT NOT NULL,\n  declared_amount DOUBLE PRECISION NOT NULL,\n  stage TEXT NOT NULL,\n  stage_history JSONB NOT NULL,\n  proofs JSONB NOT NULL,\n  disputed BOOLEAN NOT NULL,\n  dispute_reason TEXT,\n  created_at TEXT NOT NULL,\n  notes TEXT\n);\n";
/**
* Migration bookkeeping shared by the two appliers — `scripts/migrate.mjs`
* (deploy, `readdir`) and `src/lib/db.ts` (PGLite preview, `import.meta.glob`).
*
* Applied files are keyed by BASENAME, so the same file applies once no matter
* which directory it is globbed from. That is what makes the auth schema safe to
* copy from `migrations/auth/` into `migrations/` when an app turns sign-in on:
* a database that already has `0001_auth.sql` will not re-run it.
*
* Neither applier descends into subdirectories, so `migrations/auth/*.sql` is
* out of scope for both until it is copied up.
*/
/**
* The `_migrations` key for a migration path (or bare filename).
* @param {string} path
* @returns {string}
*/
function migrationName(path) {
	return path.split("/").pop() ?? path;
}
/**
* @param {string} path
* @returns {boolean}
*/
function isMigrationFile(path) {
	return path.endsWith(".sql");
}
/**
* Migrations in `paths` that are not yet in `applied`, in apply order.
* Non-`.sql` entries (a `readdir` also yields `migrations/auth/`) are dropped.
* @param {Iterable<string>} paths
* @param {Iterable<string>} applied
* @returns {Array<{ name: string, path: string }>}
*/
function pendingMigrations(paths, applied) {
	const done = new Set(applied);
	return [...paths].filter(isMigrationFile).map((path) => ({
		name: migrationName(path),
		path
	})).sort((a, b) => a.name.localeCompare(b.name)).filter(({ name }) => !done.has(name));
}
var rawDatabaseUrl = typeof process !== "undefined" ? process.env.DATABASE_URL : void 0;
var databaseUrl = rawDatabaseUrl && rawDatabaseUrl.trim() ? rawDatabaseUrl : void 0;
/**
* Active backend: real **Neon** when `DATABASE_URL` is set (deployed / configured
* sandbox), otherwise a local embedded **PGLite** (Postgres compiled to WASM) so
* the app has a working database even with nothing configured — the live preview
* included. Swap in Neon later by just setting `DATABASE_URL`; no code changes.
*/
var dbSource = databaseUrl ? "neon" : "pglite";
/**
* Init state lives on globalThis as promises: dev HMR creates new instances of
* this module, and two instances racing module-level state would open a second
* pool or run two concurrent PGLite migration passes (whose duplicate
* `_migrations` insert rejects — and would get memoized, poisoning every later
* `getSql()`). A failed init clears its slot so the next call retries.
*/
var globalRef = globalThis;
/**
* Result-type parity: Postgres sends every value as text plus a type OID — the
* JS value is the DRIVER's parsing choice, and pg and PGLite disagree (pg:
* int8 -> string, date -> local-midnight Date; PGLite: int8 -> BigInt, which
* JSON.stringify rejects, date -> UTC Date). Normalize both so preview and
* production return identical, JSON-safe shapes:
*   int8/bigint (incl. count(*)) -> number (past 2^53 loses precision — cast
*                                   `::text` if you ever need huge integers)
*   date                         -> 'YYYY-MM-DD' string
*   interval                     -> Postgres interval text
* numeric already comes back as a string on both (arbitrary precision).
*/
var OID_INT8 = 20;
var OID_DATE = 1082;
var OID_INTERVAL = 1186;
var identity = (v) => v;
/** Wrap a query runner in the tagged-template + `.query()` `Sql` surface. */
function toSql(run) {
	const sql = (async (strings, ...values) => {
		let text = strings[0];
		for (let i = 0; i < values.length; i += 1) text += `$${i + 1}${strings[i + 1]}`;
		return run(text, values);
	});
	sql.query = (text, params = []) => run(text, params);
	return sql;
}
async function runNeonMigrations(sql) {
	const migrations = /* #__PURE__ */ Object.assign({
		"/migrations/0001_auth.sql": _0001_auth_default,
		"/migrations/0002_relief.sql": _0002_relief_default
	});
	await sql.query("CREATE TABLE IF NOT EXISTS _migrations (name TEXT PRIMARY KEY, applied_at TIMESTAMPTZ NOT NULL DEFAULT now())");
	const done = (await sql.query("SELECT name FROM _migrations")).map((r) => r.name);
	const pending = pendingMigrations(Object.keys(migrations), done);
	if (pending.length === 0) return;
	console.log(`[db] Neon has ${pending.length} pending migration(s). Applying...`);
	for (const { name, path } of pending) {
		const text = migrations[path];
		await sql.query("BEGIN");
		try {
			await sql.query(text);
			await sql.query("INSERT INTO _migrations (name) VALUES ($1)", [name]);
			await sql.query("COMMIT");
			console.log(`[db] Applied Neon migration: ${name}`);
		} catch (err) {
			await sql.query("ROLLBACK");
			console.error(`[db] Failed to apply Neon migration ${name}:`, err);
			throw err;
		}
	}
}
function createNeonSql() {
	globalRef.__pgSqlPromise__ ??= (async () => {
		const { Pool, types } = await import("../_libs/pg.mjs").then((n) => n.n);
		types.setTypeParser(OID_INT8, Number);
		types.setTypeParser(OID_DATE, identity);
		types.setTypeParser(OID_INTERVAL, identity);
		const pool = new Pool({ connectionString: databaseUrl });
		const sql = toSql(async (text, params) => {
			return (await pool.query(text, params)).rows;
		});
		try {
			await runNeonMigrations(sql);
		} catch (err) {
			console.error("[db] Neon migrations failed:", err);
		}
		return sql;
	})().catch((err) => {
		globalRef.__pgSqlPromise__ = void 0;
		throw err;
	});
	return globalRef.__pgSqlPromise__;
}
async function createPgliteSql() {
	globalRef.__pgliteInstance__ ??= (async () => {
		const { PGlite } = await import("../_libs/electric-sql__pglite.mjs").then((n) => n.t);
		const pg = new PGlite({ parsers: {
			[OID_INT8]: Number,
			[OID_DATE]: identity,
			[OID_INTERVAL]: identity
		} });
		await pg.waitReady;
		await pg.exec("create table if not exists _migrations (name text primary key, applied_at timestamptz not null default now())");
		return pg;
	})().catch((err) => {
		globalRef.__pgliteInstance__ = void 0;
		throw err;
	});
	const pg = await globalRef.__pgliteInstance__;
	const migrate = async () => {
		const migrations = /* #__PURE__ */ Object.assign({
			"/migrations/0001_auth.sql": _0001_auth_default,
			"/migrations/0002_relief.sql": _0002_relief_default
		});
		const done = (await pg.query("select name from _migrations")).rows.map((r) => r.name);
		for (const { name, path } of pendingMigrations(Object.keys(migrations), done)) await pg.transaction(async (tx) => {
			await tx.exec(migrations[path]);
			await tx.query("insert into _migrations (name) values ($1)", [name]);
		});
	};
	const pass = (globalRef.__pgliteMigrateChain__ ?? Promise.resolve()).catch(() => void 0).then(migrate);
	globalRef.__pgliteMigrateChain__ = pass;
	await pass;
	return toSql(async (text, params) => {
		return (await pg.query(text, params)).rows;
	});
}
var sqlPromise = null;
async function createSql() {
	if (typeof window !== "undefined") throw new Error("@/lib/db is server-only — call getSql() from a createServerFn handler or a server route loader, never from client code.");
	return dbSource === "neon" ? createNeonSql() : createPgliteSql();
}
/**
* Get the shared, **server-only** SQL client. Neon when `DATABASE_URL` is set,
* otherwise the local PGLite fallback. Memoized — safe to call per request.
*
* Schema comes from `migrations/*.sql`, auto-applied before the first query on
* both backends — define tables there, never inline in server functions.
*/
function getSql() {
	sqlPromise ??= createSql().catch((err) => {
		sqlPromise = null;
		throw err;
	});
	return sqlPromise;
}
/**
* The shared PGLite instance (preview only), with `migrations/*.sql` applied.
* Lets Better Auth persist to the SAME embedded DB as app data in preview (via a
* Kysely dialect). Throws when `DATABASE_URL` is set (that path uses Neon).
*/
async function getPglite() {
	if (dbSource !== "pglite") throw new Error("getPglite() is only available on the PGLite fallback (no DATABASE_URL)");
	await getSql();
	const pg = await globalRef.__pgliteInstance__;
	if (!pg) throw new Error("PGLite instance failed to initialize");
	return pg;
}
/**
* Finish DB bootstrap before the server handles traffic.
*
* - **PGLite** (preview / no `DATABASE_URL`): open the in-memory DB and apply
*   `migrations/*.sql`. Idempotent — concurrent callers share one promise.
* - **Neon**: no-op (pool is created lazily on first query).
*
* Vite `configureServer` awaits this at dev startup; production imports of this
* module kick it off immediately (see bottom of file).
*/
function ensureDbReady() {
	if (dbSource !== "pglite") return Promise.resolve();
	return getSql().then(() => void 0);
}
var globalBoot = globalThis;
if (typeof window === "undefined" && dbSource === "pglite") globalBoot.__pgBootstrapPromise__ ??= ensureDbReady().catch((err) => {
	globalBoot.__pgBootstrapPromise__ = void 0;
	console.error("[db] PGLite bootstrap failed:", err);
	throw err;
});
/**
* Auth middleware for server functions — the standard way to get the caller's
* verified user id. When deployed the session cookie is same-origin and rides
* along automatically. In the live preview the client also forwards the bearer
* token (partitioned cookies) via the `.client` hook below — call sites do not
* thread it themselves.
*
*   import { createServerFn } from "@tanstack/react-start";
*   import { getSql } from "@/lib/db";
*   import { authMiddleware } from "@/lib/auth/middleware";
*
*   export const listTodos = createServerFn({ method: "GET" })
*     .middleware([authMiddleware])
*     .handler(async ({ context }) => {
*       const sql = await getSql();
*       return sql`select * from todos where user_id = ${context.userId}`;
*     });
*
* Signed out with auth on (live preview included) -> throws `UnauthorizedError`
* (see `verify.server.ts`). With auth disabled (`VITE_AUTH_ENABLED=false`, the
* shipped default) it resolves the shared dev user — but throws instead when a
* `DATABASE_URL` is also set, so an app without sign-in must not use this at
* all. On the auth-on path, use it on every server function that touches
* per-user data and scope every query by `context.userId`.
*/
var authMiddleware = createMiddleware({ type: "function" }).client(async ({ next }) => {
	const { getBearerToken } = await import("./client-KRVwBcS2.mjs").then((n) => n.n);
	return next({ sendContext: { bearerToken: getBearerToken() ?? void 0 } });
}).server(async ({ next, context }) => {
	const { assertSameSiteRequest } = await import("./isolation.server-CGNg1r0B.mjs");
	const { requireUserId } = await import("./verify.server-CHiuq47A.mjs");
	assertSameSiteRequest();
	return next({ context: { userId: await requireUserId(context.bearerToken) } });
});
/**
* Rule-based "AI Priority Score" for the prototype.
* Weighted signals: people at risk, unfilled resource gap, time pressure, stated urgency.
*/
function computePriorityScore(input) {
	const fulfilled = input.quantityFulfilled ?? 0;
	const remainRatio = input.quantityRequired <= 0 ? 0 : Math.max(0, Math.min(1, 1 - fulfilled / input.quantityRequired));
	const people = Math.round(Math.min(30, input.peopleAffected / 80));
	const gap = Math.round(remainRatio * 20);
	const time = Math.round(Math.max(0, 15 - input.durationDays));
	const urgency = Math.round(Math.max(1, Math.min(5, input.urgency)) * 7);
	const raw = people + gap + time + urgency;
	const score = Math.max(1, Math.min(100, raw));
	let priority;
	if (score >= 78) priority = "critical";
	else if (score >= 58) priority = "high";
	else if (score >= 40) priority = "moderate";
	else priority = "low";
	return {
		score,
		priority,
		breakdown: {
			people,
			gap,
			time,
			urgency
		}
	};
}
function mockVerifyAmount(entered, expected) {
	if (!entered || entered <= 0) return "pending_review";
	if (Math.abs(entered - expected) / Math.max(expected, 1) > .08) return "mismatch_flagged";
	return "verified";
}
var hoursAgo = (h) => (/* @__PURE__ */ new Date(Date.now() - h * 36e5)).toISOString();
function req(partial) {
	const scored = computePriorityScore({
		peopleAffected: partial.peopleAffected,
		quantityRequired: partial.quantityNeeded,
		quantityFulfilled: partial.quantityFulfilled,
		durationDays: partial.durationDays,
		urgency: partial.urgency
	});
	const status = partial.quantityNeeded - partial.quantityFulfilled <= 0 ? "fulfilled" : partial.quantityFulfilled > 0 ? "partial" : "open";
	return {
		...partial,
		status,
		priority: scored.priority,
		priorityScore: scored.score,
		scoreBreakdown: scored.breakdown
	};
}
var seedUsers = [
	{
		id: "u-ananya",
		name: "Ananya Rao",
		role: "donor",
		contributionType: "food",
		location: "Bengaluru"
	},
	{
		id: "u-priya",
		name: "Priya Shah",
		role: "donor",
		contributionType: "medical",
		location: "Hyderabad"
	},
	{
		id: "u-ravi",
		name: "Ravi Menon",
		role: "receiver",
		orgName: "Camp Sundari",
		location: "East Bank"
	},
	{
		id: "u-fatima",
		name: "Fatima Begum",
		role: "receiver",
		orgName: "Camp Lakshmi",
		location: "West Marsh"
	},
	{
		id: "u-dev",
		name: "Dev Patel",
		role: "receiver",
		orgName: "Camp Meera",
		location: "North Ridge"
	},
	{
		id: "u-nisha",
		name: "Nisha Verma",
		role: "receiver",
		orgName: "Camp Anjuman",
		location: "River Fork"
	},
	{
		id: "u-arjun",
		name: "Arjun Das",
		role: "receiver",
		orgName: "Camp Rohan",
		location: "South Fields"
	},
	{
		id: "u-sara",
		name: "Sara Joseph",
		role: "receiver",
		orgName: "Camp Noor",
		location: "Highway 9"
	},
	{
		id: "u-leila",
		name: "Leila Hassan",
		role: "coordinator",
		region: "Kaveri Basin",
		location: "Basin HQ"
	},
	{
		id: "u-omar",
		name: "Omar Khalid",
		role: "coordinator",
		region: "Highway Corridor",
		location: "Mile 14 Depot"
	}
];
var seedRequirements = [
	req({
		id: "req-lakshmi-water",
		title: "Drinking water for West Marsh",
		campName: "Camp Lakshmi",
		receiverId: "u-fatima",
		resourceType: "water",
		quantityNeeded: 1e3,
		quantityUnit: "litres",
		quantityFulfilled: 0,
		peopleAffected: 2e3,
		durationDays: 1,
		urgency: 5,
		location: "West Marsh",
		mapX: 18,
		mapY: 42,
		notes: "Embankment breach overnight. Hand pumps silted. Children under five: 310. Tanker access possible via the west levee road until 18:00.",
		createdAt: hoursAgo(4)
	}),
	req({
		id: "req-sundari-food",
		title: "Kitchen tents out of rations",
		campName: "Camp Sundari",
		receiverId: "u-ravi",
		resourceType: "food",
		quantityNeeded: 500,
		quantityUnit: "packets",
		quantityFulfilled: 120,
		peopleAffected: 1200,
		durationDays: 3,
		urgency: 5,
		location: "East Bank",
		mapX: 72,
		mapY: 38,
		notes: "One gas cylinder remaining. 180 children under five. Ready-to-eat packets preferred — no dry fuel on site.",
		createdAt: hoursAgo(9)
	}),
	req({
		id: "req-noor-hygiene",
		title: "Hygiene kits after overflow",
		campName: "Camp Noor",
		receiverId: "u-sara",
		resourceType: "hygiene",
		quantityNeeded: 80,
		quantityUnit: "kits",
		quantityFulfilled: 20,
		peopleAffected: 300,
		durationDays: 5,
		urgency: 4,
		location: "Highway 9",
		mapX: 82,
		mapY: 62,
		notes: "Latrine overflow after the second crest. Skin infections starting in the south row of tents.",
		createdAt: hoursAgo(14)
	}),
	req({
		id: "req-meera-clothing",
		title: "Blankets for North Ridge nights",
		campName: "Camp Meera",
		receiverId: "u-dev",
		resourceType: "clothing",
		quantityNeeded: 200,
		quantityUnit: "blankets",
		quantityFulfilled: 40,
		peopleAffected: 400,
		durationDays: 7,
		urgency: 4,
		location: "North Ridge",
		mapX: 48,
		mapY: 18,
		notes: "Elevation is dry but nights drop to 14°C. Elderly ward has no floor insulation.",
		createdAt: hoursAgo(20)
	}),
	req({
		id: "req-anjuman-medical",
		title: "Field clinic restock",
		campName: "Camp Anjuman",
		receiverId: "u-nisha",
		resourceType: "medical",
		quantityNeeded: 50,
		quantityUnit: "kits",
		quantityFulfilled: 10,
		peopleAffected: 80,
		durationDays: 2,
		urgency: 3,
		location: "River Fork",
		mapX: 40,
		mapY: 48,
		notes: "ORS, antiseptic, and tetanus dressings. Clinic tent is standing; only stock is short.",
		createdAt: hoursAgo(6)
	}),
	req({
		id: "req-rohan-shelter",
		title: "Family tents for South Fields",
		campName: "Camp Rohan",
		receiverId: "u-arjun",
		resourceType: "shelter",
		quantityNeeded: 30,
		quantityUnit: "tents",
		quantityFulfilled: 6,
		peopleAffected: 150,
		durationDays: 14,
		urgency: 2,
		location: "South Fields",
		mapX: 55,
		mapY: 78,
		notes: "Ground is draining. Families currently sharing tarps. Not an immediate medical risk.",
		createdAt: hoursAgo(30)
	})
];
var seedSurplus = [
	{
		id: "sur-anjuman-blankets",
		campName: "Camp Anjuman",
		receiverId: "u-nisha",
		resourceType: "clothing",
		quantity: 80,
		quantityUnit: "blankets",
		location: "River Fork",
		mapX: 40,
		mapY: 48,
		notes: "Winter stock never issued. Dry, bagged, ready to load.",
		createdAt: hoursAgo(8),
		status: "available"
	},
	{
		id: "sur-rohan-food",
		campName: "Camp Rohan",
		receiverId: "u-arjun",
		resourceType: "food",
		quantity: 200,
		quantityUnit: "packets",
		location: "South Fields",
		mapX: 55,
		mapY: 78,
		notes: "Rice packets from a delayed convoy. We cannot store past Friday.",
		createdAt: hoursAgo(5),
		status: "available"
	},
	{
		id: "sur-meera-water",
		campName: "Camp Meera",
		receiverId: "u-dev",
		resourceType: "water",
		quantity: 400,
		quantityUnit: "litres",
		location: "North Ridge",
		mapX: 48,
		mapY: 18,
		notes: "Spring-fed tank at capacity. Jerrycans available for transfer.",
		createdAt: hoursAgo(11),
		status: "available"
	}
];
var seedTransactions = [
	{
		id: "tx-1001",
		kind: "donation",
		requirementId: "req-sundari-food",
		donorId: "u-ananya",
		donorName: "Ananya Rao",
		receiverId: "u-ravi",
		receiverName: "Ravi Menon",
		campName: "Camp Sundari",
		resourceType: "food",
		quantity: 120,
		quantityUnit: "packets",
		declaredAmount: 120,
		stage: "donated",
		stageHistory: [{
			stage: "pending",
			at: hoursAgo(8),
			by: "Ananya Rao"
		}, {
			stage: "donated",
			at: hoursAgo(7.5),
			by: "Ananya Rao"
		}],
		proofs: [{
			id: "pf-1001-a",
			uploadedBy: "donor",
			uploaderName: "Ananya Rao",
			kind: "receipt",
			fileName: "kirana-receipt.jpg",
			dataUrl: "",
			mimeType: "image/jpeg",
			amountEntered: 120,
			uploadedAt: hoursAgo(7.4),
			verification: "verified"
		}],
		disputed: false,
		createdAt: hoursAgo(8),
		notes: "Packed at the east warehouse. Awaiting coordinator pickup."
	},
	{
		id: "tx-1002",
		kind: "donation",
		requirementId: "req-anjuman-medical",
		donorId: "u-priya",
		donorName: "Priya Shah",
		coordinatorId: "u-leila",
		coordinatorName: "Leila Hassan",
		receiverId: "u-nisha",
		receiverName: "Nisha Verma",
		campName: "Camp Anjuman",
		resourceType: "medical",
		quantity: 10,
		quantityUnit: "kits",
		declaredAmount: 10,
		stage: "in_transit",
		stageHistory: [
			{
				stage: "pending",
				at: hoursAgo(16),
				by: "Priya Shah"
			},
			{
				stage: "donated",
				at: hoursAgo(15),
				by: "Priya Shah"
			},
			{
				stage: "received_by_coordinator",
				at: hoursAgo(12),
				by: "Leila Hassan"
			},
			{
				stage: "in_transit",
				at: hoursAgo(3),
				by: "Leila Hassan"
			}
		],
		proofs: [{
			id: "pf-1002-a",
			uploadedBy: "donor",
			uploaderName: "Priya Shah",
			kind: "invoice",
			fileName: "clinic-invoice.pdf",
			dataUrl: "",
			mimeType: "application/pdf",
			amountEntered: 10,
			uploadedAt: hoursAgo(15),
			verification: "verified"
		}, {
			id: "pf-1002-b",
			uploadedBy: "coordinator",
			uploaderName: "Leila Hassan",
			kind: "delivery_bill",
			fileName: "waybill-14.jpg",
			dataUrl: "",
			mimeType: "image/jpeg",
			amountEntered: 10,
			uploadedAt: hoursAgo(3),
			verification: "verified"
		}],
		disputed: false,
		createdAt: hoursAgo(16),
		notes: "Boat crossing at River Fork — ETA before dusk."
	},
	{
		id: "tx-1003",
		kind: "donation",
		requirementId: "req-noor-hygiene",
		donorId: "u-ananya",
		donorName: "Ananya Rao",
		coordinatorId: "u-omar",
		coordinatorName: "Omar Khalid",
		receiverId: "u-sara",
		receiverName: "Sara Joseph",
		campName: "Camp Noor",
		resourceType: "hygiene",
		quantity: 20,
		quantityUnit: "kits",
		declaredAmount: 20,
		stage: "delivered",
		stageHistory: [
			{
				stage: "pending",
				at: hoursAgo(40),
				by: "Ananya Rao"
			},
			{
				stage: "donated",
				at: hoursAgo(38),
				by: "Ananya Rao"
			},
			{
				stage: "received_by_coordinator",
				at: hoursAgo(30),
				by: "Omar Khalid"
			},
			{
				stage: "in_transit",
				at: hoursAgo(22),
				by: "Omar Khalid"
			},
			{
				stage: "delivered",
				at: hoursAgo(6),
				by: "Sara Joseph"
			}
		],
		proofs: [{
			id: "pf-1003-a",
			uploadedBy: "donor",
			uploaderName: "Ananya Rao",
			kind: "receipt",
			fileName: "hygiene-challan.jpg",
			dataUrl: "",
			mimeType: "image/jpeg",
			amountEntered: 20,
			uploadedAt: hoursAgo(38),
			verification: "verified"
		}, {
			id: "pf-1003-c",
			uploadedBy: "receiver",
			uploaderName: "Sara Joseph",
			kind: "delivery_bill",
			fileName: "gate-pass.jpg",
			dataUrl: "",
			mimeType: "image/jpeg",
			amountEntered: 20,
			uploadedAt: hoursAgo(6),
			verification: "verified"
		}],
		disputed: false,
		createdAt: hoursAgo(40),
		notes: "Unloaded at Highway 9 gate. Awaiting distribution record."
	},
	{
		id: "tx-1004",
		kind: "donation",
		requirementId: "req-lakshmi-water",
		donorId: "u-priya",
		donorName: "Priya Shah",
		coordinatorId: "u-leila",
		coordinatorName: "Leila Hassan",
		receiverId: "u-fatima",
		receiverName: "Fatima Begum",
		campName: "Camp Lakshmi",
		resourceType: "water",
		quantity: 200,
		quantityUnit: "litres",
		declaredAmount: 200,
		stage: "delivered",
		stageHistory: [
			{
				stage: "pending",
				at: hoursAgo(18),
				by: "Priya Shah"
			},
			{
				stage: "donated",
				at: hoursAgo(17),
				by: "Priya Shah"
			},
			{
				stage: "received_by_coordinator",
				at: hoursAgo(14),
				by: "Leila Hassan"
			},
			{
				stage: "in_transit",
				at: hoursAgo(10),
				by: "Leila Hassan"
			},
			{
				stage: "delivered",
				at: hoursAgo(2),
				by: "Fatima Begum"
			}
		],
		proofs: [{
			id: "pf-1004-a",
			uploadedBy: "donor",
			uploaderName: "Priya Shah",
			kind: "invoice",
			fileName: "tanker-invoice.pdf",
			dataUrl: "",
			mimeType: "application/pdf",
			amountEntered: 200,
			uploadedAt: hoursAgo(17),
			verification: "verified"
		}, {
			id: "pf-1004-b",
			uploadedBy: "receiver",
			uploaderName: "Fatima Begum",
			kind: "delivery_bill",
			fileName: "dipstick-log.jpg",
			dataUrl: "",
			mimeType: "image/jpeg",
			amountEntered: 140,
			uploadedAt: hoursAgo(2),
			verification: "mismatch_flagged"
		}],
		disputed: true,
		disputeReason: "Declared 200 litres. Receiver dipstick log records 140 litres. Auto-flagged for coordinator review.",
		createdAt: hoursAgo(18),
		notes: "Tanker arrived short. Dipstick reading does not match invoice."
	},
	{
		id: "tx-1005",
		kind: "donation",
		requirementId: "req-meera-clothing",
		donorId: "u-ananya",
		donorName: "Ananya Rao",
		coordinatorId: "u-leila",
		coordinatorName: "Leila Hassan",
		receiverId: "u-dev",
		receiverName: "Dev Patel",
		campName: "Camp Meera",
		resourceType: "clothing",
		quantity: 40,
		quantityUnit: "blankets",
		declaredAmount: 40,
		stage: "confirmed",
		stageHistory: [
			{
				stage: "pending",
				at: hoursAgo(72),
				by: "Ananya Rao"
			},
			{
				stage: "donated",
				at: hoursAgo(70),
				by: "Ananya Rao"
			},
			{
				stage: "received_by_coordinator",
				at: hoursAgo(60),
				by: "Leila Hassan"
			},
			{
				stage: "in_transit",
				at: hoursAgo(50),
				by: "Leila Hassan"
			},
			{
				stage: "delivered",
				at: hoursAgo(36),
				by: "Dev Patel"
			},
			{
				stage: "confirmed",
				at: hoursAgo(24),
				by: "Dev Patel"
			}
		],
		proofs: [{
			id: "pf-1005-a",
			uploadedBy: "donor",
			uploaderName: "Ananya Rao",
			kind: "receipt",
			fileName: "blanket-receipt.jpg",
			dataUrl: "",
			mimeType: "image/jpeg",
			amountEntered: 40,
			uploadedAt: hoursAgo(70),
			verification: "verified"
		}, {
			id: "pf-1005-d",
			uploadedBy: "receiver",
			uploaderName: "Dev Patel",
			kind: "distribution_record",
			fileName: "ward-list.pdf",
			dataUrl: "",
			mimeType: "application/pdf",
			amountEntered: 40,
			uploadedAt: hoursAgo(24),
			verification: "verified"
		}],
		disputed: false,
		createdAt: hoursAgo(72),
		notes: "Distributed to the elderly ward. Closed."
	}
];
//#endregion
export { getSql as a, seedSurplus as c, getPglite as i, seedTransactions as l, computePriorityScore as n, mockVerifyAmount as o, ensureDbReady as r, seedRequirements as s, authMiddleware as t, seedUsers as u };
